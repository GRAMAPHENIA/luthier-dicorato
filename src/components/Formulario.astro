<form id="contacto">
  <h1>
    Contactate con nosotros, <span>completando</span> el siguiente <code
      >formulario</code
    >.
  </h1>
  <fieldset>
    <legend>Consultas</legend>
    <label for="name">Nombre</label>
    <input type="text" id="name" name="name" />
    <label for="email">Email</label>
    <input type="email" id="email" name="email" />
    <label for="email">Consulta</label>
    <textarea name="" id="" cols="30" rows="10"></textarea>
    <button class="detalles">enviar</button>
  </fieldset>
</form>

<style>
  h1 {
    text-align: center;
    margin: 0 2rem;
  }

  form {
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
    justify-content: center;
    margin: 0 auto;
    max-width: 600px;
    
  }

  fieldset {
    background: var(--fondo-parrafo);
    display: flex;
    flex-direction: column;
    border-color: var(--borde-parrafo);
    border-radius: 0.25rem;
    -webkit-border-radius: 0.25rem;
    -moz-border-radius: 0.25rem;
    -ms-border-radius: 0.25rem;
    -o-border-radius: 0.25rem;
    padding: 2rem;
    margin: 3rem 0;
  }

  legend {
    text-align: left;
    padding-inline: 0.5rem;
    color: aliceblue;
    font-size: 22px;
  }

  code {
    background-color: #eee;
    color: rgb(45, 55, 63);
    border-radius: 3px;
    font-family: courier, monospace;
    padding: 0 3px;
  }

  span {
    color: salmon;
  }

  input {
    padding: 0.5rem;
    background: var(--fondo-dark);
    border: 2px solid var(--borde-parrafo);
    border-radius: 0.25rem;
    margin-bottom: 2rem;
    color: aliceblue;
    font-size: 16px;
  }
  textarea {
    padding: 0.5rem;
    background: var(--fondo-dark);
    border: 2px solid var(--borde-parrafo);
    border-radius: 0.25rem;
    margin-bottom: 2rem;
    color: aliceblue;
    font-size: 16px;
  }

  label {
    font-size: 18px;
    padding-bottom: 0.5rem;
  }

  .detalles {
    background: #2e3c4ad3;
    border: 2px solid #3d5874d3;
    color: rgba(240, 248, 255, 0.822);
    padding: 10px;
    margin-top: 10px;
    width: 200px;
    cursor: pointer;
    border-radius: 0.25rem;
    font-size: 20px;
  }

  .detalles:hover {
    background: #3d566ed3;
    color: white;
  }

  @media (max-width: 768px) {
    form {
      margin: .75rem;
    }
  }
</style>
